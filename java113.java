package chap07;

public class Earth {
    // 상수 --> 일반적으로 불변의 값(static final)
    // 상수 선언
    static final double EARTH_RADIUS = 6400;
    static final double EARTH_AREA = 4 * Math.PI * EARTH_RADIUS * EARTH_RADIUS;
}
